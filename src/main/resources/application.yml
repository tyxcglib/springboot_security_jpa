server:
  port: 9003
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/securitymybatis?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
  thymeleaf:
    cache: false
logging:
  level:
    org.springframework.security: debug
